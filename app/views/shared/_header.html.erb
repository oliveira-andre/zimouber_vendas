<nav class="navbar" role="navigation" aria-label="main navigation">
  <div class="navbar-brand">
    <%= link_to t('header.begin'), root_path, class: 'navbar-item'%>
    <a role="button" class="navbar-burger burger" aria-label="menu" aria-expanded="false" data-target="main_navbar">
      <span aria-hidden="true"></span>
      <span aria-hidden="true"></span>
      <span aria-hidden="true"></span>
    </a>
  </div>

  <div id="main_navbar" class="navbar-menu">
    <div class="navbar-end">
      <div class="navbar-item">
        <div class="buttons">
          <% if establishment_signed_in? %>
            <%= link_to t('header.my_account'), advertisements_path, class: 'button is-primary'%>
            <%= link_to t('header.loggout'), destroy_establishment_session_path, class: 'button is-danger', method: :delete %>
          <% else %>
            <%= link_to t('header.sign_up'), new_establishment_registration_path, class: 'button is-primary' %>
            <%= link_to t('header.login'), new_establishment_session_path, class: 'button' %>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</nav>